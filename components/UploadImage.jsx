{/* <div>
        <form>
          <input
            type="file"
            name="img"
            placeholder="image"
            onChange={(e) => {
              console.log(e.target.files);
              setFile(URL.createObjectURL(e.target.files[0]));
            }}
          />
        </form>

        <img src={file} alt="" />
      </div> */}